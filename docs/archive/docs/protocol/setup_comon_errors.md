# Setup — Common Errors (legacy)

> **Deprecated.** This document collected the typical errors
> encountered when starting CHIPS + c-lightning + the old `bet`
> binary together (`lightning-cli: Connection refused`,
> `chipsd` not synced, `unable make lock_in transaction`).
> None of those failure modes apply to the current Verus-only
> build: there is no Lightning daemon, no `chipsd`, and no
> lock-in multisig transaction.
>
> The current operator-facing troubleshooting lives in:
>
> * [`docs/VRSCTEST_LOCAL_SETUP.md`](../../../tutorials/cli-auto-vrsctest.md) —
>   regtest bring-up, daemon flags, identity registration,
>   reset/recovery procedures.
> * [`docs/COMMUNITY_TESTING_GUIDE.md`](../../../tutorials/community-quickstart.md)
>   — sanity checks for new contributors.
>
> This file is kept only to preserve inbound links during the
> documentation reorganisation and will be archived under
> `docs/archive/` in a follow-up cleanup.
