# Ubuntu Compilation (legacy)

> **Deprecated.** This document described the old CHIPS + c-lightning
> + `bet` build sequence on Ubuntu 18.04/20.04. That stack is no
> longer used: Lightning support has been removed from `bet`, and
> the chain backend is now Verus (`VRSCTEST` on the dev regtest)
> instead of CHIPS.
>
> The current Ubuntu build steps for `bet` itself are in
> [`compile.md`](../../../guides/build-from-source.md). For the chain side, see
> [`docs/VRSCTEST_LOCAL_SETUP.md`](../../../tutorials/cli-auto-vrsctest.md).
>
> This file is kept only to preserve inbound links during the
> documentation reorganisation and will be archived under
> `docs/archive/` in a follow-up cleanup.
